name: Scrape bonuses, spawns and effects

on:
  schedule:
    - cron: "15 */2 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -r requirements.txt

      - run: python scrape.py

      - name: Commit changed feeds
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git diff --quiet -- bonuses.json spawns.json effects.json; then
            echo "No changes."
          else
            git add bonuses.json spawns.json effects.json
            git commit -m "Update bonuses/spawns/effects feeds"
            git push
          fi

